project:
  name: EcoTienda
  root: /home/sebas/Projects/EcoTienda
  description: Internal web platform for EcoTienda operations.

artifact_store:
  mode: openspec
  backend: filesystem
  root: openspec

sdd:
  enabled: true
  strict_tdd: false
  phase_order:
    - proposal
    - spec
    - design
    - tasks
    - apply
    - verify
    - archive
  phase_rules:
    proposal: Capture the business problem, target users, scope, non-goals, constraints, and tradeoffs.
    spec: Translate the approved proposal into functional requirements and acceptance criteria.
    design: Define the architecture and technical decisions that satisfy the spec.
    tasks: Break the spec and design into small reviewable implementation steps.
    apply: Implement tasks in small batches and preserve progress between batches.
    verify: Validate implementation against the spec, tasks, and acceptance criteria.
    archive: Record the final outcome and close the change.

testing:
  test_runner: none
  validation_commands:
    - npm run lint
    - npm run build
  notes: No dedicated test script is configured yet. Use lint and build as the current safety net.

stack:
  framework: Next.js
  language: TypeScript
  ui: Tailwind CSS
  backend: Supabase
  auth: Supabase Auth
  database: Supabase Postgres
  storage: Supabase Storage
  maps: Google Maps API

sources_of_truth:
  - README.md
  - docs/development-plan.md
  - docs/phase-0-bootstrap.md

current_context:
  phase: archive
  focus: keep the legacy redesign slices out of the active queue while recording their real archive blockers before starting the workflow-based product model
  priorities:
    - preserve truthful closure blockers for prelogin-auth-redesign
    - preserve truthful closure blockers for internal-panel-foundation-redesign
    - decide the next workflow-based product-model change after clearing lifecycle noise
  next_feature: decide the next workflow-based product-model change after lifecycle cleanup
  active_changes:
    - name: formato-cfe
      status: planning_only
      next: spec
      blocker: missing specs, design, and tasks artifacts
  blocked_changes:
    - name: prelogin-auth-redesign
      status: archive_blocked
      next: rerun verification only after manual viewport/auth parity evidence is captured and cleanup task 5.1 is resolved, then obtain truthful native review authority
      blocker: verify-report.md is FAIL with unchecked tasks 4.2, 4.3, and 5.1, and the change has no native review receipt artifacts under openspec/changes/prelogin-auth-redesign/
    - name: internal-panel-foundation-redesign
      status: archive_blocked
      next: produce a truthful verify-report, complete or explicitly resolve tasks 13 and 15-21, then obtain truthful native review authority
      blocker: the change has unchecked tasks 13 and 15-21, no verify-report.md, and no native review receipt artifacts under openspec/changes/internal-panel-foundation-redesign/
  archived_changes:
    - 2026-07-10-ubicacion-cliente
    - 2026-07-10-geolocalizacion-cliente
    - 2026-07-11-diagrama-unifilar-datos
    - 2026-07-12-selector-cliente-directo
    - 2026-07-12-pdf-ubicacion-cliente
    - 2026-07-12-home-mobile-polish
    - 2026-07-12-mobile-shell-polish
    - 2026-07-13-agenda-v1
